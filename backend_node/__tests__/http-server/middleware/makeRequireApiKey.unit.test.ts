describe("makeRequireApiKey Middleware", () => {
  it("should always be passing", () => {
    expect(true).toBeTruthy();
  });
  it.todo("should disallow unauthenticated requests");
  it.todo("should allow authenticated requests to go through");
});
